[project]
name = "lab9-agent-avec-langgraph"
version = "0.1.0"
description = "LAB: Agent avec LangGraph"
requires-python = ">=3.10"
dependencies = [
    "langchain",
    "langchain-ollama",
    "langgraph",
    "python-dotenv",
]

[tool.uv]
package = false
